const Main = ({ tNum }: { tNum: number }) => {
  return (
    <p className="m-auto px-1 py-2 text-center text-[8px] text-gradient-yellow-100 md:px-5 md:py-5 md:text-xs">
      Table 0{tNum}
    </p>
  );
};

export default Main;
